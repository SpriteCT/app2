# API Endpoints - Краткая сводка

## 📊 Реализованные эндпоинты

### 🏢 Клиенты (`/api/clients`)
✅ GET `/api/clients` - Список клиентов  
✅ GET `/api/clients/{id}` - Информация о клиенте  
✅ POST `/api/clients` - Создать клиента  
✅ PUT `/api/clients/{id}` - Обновить клиента  
✅ DELETE `/api/clients/{id}` - Удалить клиента  
✅ GET `/api/clients/{id}/projects` - Проекты клиента  
✅ POST `/api/clients/{id}/projects` - Создать проект  

### 🛡️ Уязвимости (`/api/vulnerabilities`)
✅ GET `/api/vulnerabilities` - Список (с фильтрами)  
✅ GET `/api/vulnerabilities/{id}` - Информация об уязвимости  
✅ POST `/api/vulnerabilities` - Создать вручную  
✅ POST `/api/vulnerabilities/import` - Импорт из сканера  
✅ GET `/api/vulnerabilities/stats/criticality` - Статистика  
**Фильтры:** criticality, status, client, search

### 🎫 Тикеты (`/api/tickets`)
✅ GET `/api/tickets` - Список (с фильтрами)  
✅ GET `/api/tickets/{id}` - Информация о тикете  
✅ POST `/api/tickets` - Создать тикет  
✅ PUT `/api/tickets/{id}` - Обновить тикет  
✅ POST `/api/tickets/{id}/messages` - Добавить сообщение в чат  
✅ GET `/api/tickets/stats/priority` - Статистика по приоритетам  
✅ GET `/api/tickets/stats/status` - Статистика по статусам  
**Фильтры:** priority, status, client, search

### 💻 Активы (`/api/assets`)
✅ GET `/api/assets` - Список (с фильтрами)  
✅ GET `/api/assets/{id}` - Информация об активе  
✅ POST `/api/assets` - Добавить актив  
✅ POST `/api/assets/import` - Импорт из CMDB  
✅ GET `/api/assets/export/csv` - Экспорт в CSV  
**Фильтры:** type, status, criticality, search

### 📈 Отчёты (`/api/reports`)
✅ GET `/api/reports/dashboard` - Данные дашборда  
✅ GET `/api/reports/vulnerability-trend` - Тренд уязвимостей  
✅ GET `/api/reports/assets-distribution` - Распределение активов  
✅ POST `/api/reports/generate` - Сгенерировать отчёт  
✅ GET `/api/reports/templates` - Шаблоны отчётов  

## 📋 Статус реализации

### Реализовано:
- ✅ 5 роутеров с полным CRUD
- ✅ Фильтрация и поиск по всем сущностям
- ✅ Эндпоинты для статистики
- ✅ Импорт/экспорт данных
- ✅ Чат для тикетов
- ✅ Дашборд с аналитикой
- ✅ CORS настроен
- ✅ Swagger документация автогенерируется

### Планируется:
- ⏳ База данных (PostgreSQL)
- ⏳ Аутентификация (JWT)
- ⏳ Real-time обновления (WebSocket)
- ⏳ Генерация PDF/Excel отчётов
- ⏳ Парсинг файлов сканеров
- ⏳ Redis для кэширования
- ⏳ Логирование
- ⏳ Unit/Integration тесты

## 🚀 Быстрый старт

```bash
# Установить зависимости
pip install -r backend/requirements.txt

# Запустить сервер
cd backend
uvicorn main:app --reload

# API документация
open http://localhost:8000/docs

# Тестировать эндпоинт
curl http://localhost:8000/api/clients
```

